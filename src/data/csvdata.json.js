// data = (FileAttachment("./data/preprocessed.csv").csv()).map(d => ({
//             state: d.state,
//             cluster: +d.flooded_cluster_default,  // Convert to number
//             inertia: +d.inertia_result_pca_i,
//             lat: +d.latitude,
//             lon: +d.longitude
//           }));